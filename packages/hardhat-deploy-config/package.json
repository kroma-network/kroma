{
  "name": "@kroma/hardhat-deploy-config",
  "version": "1.0.0",
  "description": "[Kroma] Hardhat deploy configuration plugin",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist/*"
  ],
  "scripts": {
    "test:coverage": "echo 'No tests defined.'",
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist/ ./tsconfig.tsbuildinfo",
    "lint": "pnpm lint:fix && pnpm lint:check",
    "pre-commit": "lint-staged",
    "lint:fix": "pnpm lint:check --fix",
    "lint:check": "eslint . --max-warnings=0"
  },
  "keywords": [
    "config",
    "deploy",
    "ethereum",
    "hardhat",
    "kroma",
    "plugin"
  ],
  "homepage": "https://github.com/kroma-network/kroma/tree/dev/packages/hardhat-deploy-config#readme",
  "license": "MIT",
  "author": "Lightscale Inc.",
  "repository": {
    "type": "git",
    "url": "https://github.com/kroma-network/kroma.git"
  },
  "devDependencies": {
    "ethers": "^5.7.0",
    "hardhat": "^2.9.6",
    "ts-node": "^10.9.1"
  }
}
